


<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <h1><a href="{{ path('Accueil') }}"><img src="{{ asset('logo/made-in-loire.png') }}" alt="image" class="logo"></a></h1>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('Administration') }}">Accueil <span class="sr-only"></span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Tous nos Produits</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Qui somme nous ? </a>
            </li>
            <li class="nav-item ">
                <a class="nav-link" href="#">Panier</a>
            </li>
            {% if app.user %}
                {% if app.user.entreprise%}
                    <a class="nav-link" href="{{ path('Partenaire') }}">Mon entreprise </a>
                {% else%}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('Partenaire') }}">Devenir partenaire </a>
                </li>
                {% endif %}
            {% else %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
            </li>
            {% endif %}
        </ul>
    </div>
</nav>
