
<link rel="stylesheet" href="{{ asset('css/made-in-loire.css') }}">

<nav class="navbar navbar-expand-lg navbar-light bg-white" >
    <h1><a href="{{ path('Accueil') }}"><img src="{{ asset('logo/made-in-loire.png') }}" alt="image"></a></h1>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('Administration') }}">Accueil <span class="sr-only"></span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Tous nos Produits</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Qui somme nous ? </a>
            </li>
            {% if app.user %}
                {% if app.user.entreprise %}
                    <a class="nav-link" href="{{ path('Partenaire') }}">Mon entreprise </a>
                    <a class="nav-link" href="{{ path('produitPartenaire') }}">Mes produits</a>
                {% else%}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('InscriptionFournisseur') }}">Devenir partenaire </a>
                    </li>
                {% endif %}
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                </li>
            {% endif %}
            <li class="nav-item ">
                <a class="nav-link" href="#">Panier</a>
            </li>
            {% if app.user %}
                <li class="nav-item ">
                    <a class="nav-link" href="{{ path('app_logout') }}">Se deconnecter</a>
                </li>
            {% else %}
            <li class="nav-item ">
                <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
            </li>
            {% endif %}

        </ul>
    </div>
</nav>
